IF TEST -n "$SHELL"
FS ALLOW READ $SHELL
EXEC ALLOW $SHELL
END
IF TEST -n "$XDG_DATA_HOME"
FS ALLOW WRITE $XDG_DATA_HOME/fish/fish_history
ELSE
FS ALLOW WRITE $HOME/.local/share/fish/fish_history
END
