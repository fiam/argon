[package]
name = "argon-lib"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "argon_lib"
crate-type = ["rlib", "staticlib"]

[dependencies]
argon-core = { path = "../argon-core" }
libc = "0.2"
uuid.workspace = true

[dev-dependencies]
tempfile = "3"
